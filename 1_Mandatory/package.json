{
  "dependencies": {
    "cross-env": "^7.0.2",
    "express": "^4.17.1",
    "node-fetch": "^2.6.1"
  },
  "devDependencies": {
    "encoding": "^0.1.13",
    "nodemon": "^2.0.5"
  },
  "scripts": {
    "start-dev": "cross-env PORT=8080 nodemon app.js",
    "start-prod": "cross-env PORT=5000 node app.js"
  }
}
